<h1>Read Messages</h1>
<% if @messages.blank? %>
  <p>No read messages has been registered</p>
<% else %>
  <div class="ui cards">
    <% @messages.each do |message| %>
      <%= render "message", message: message %>
    <% end %>
  </div>
<% end %>